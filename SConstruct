env = Environment(CXXFLAGS="-std=c++0x");
MainLibraryLinks = [ 'SDL2main', 'SDL2']
env.Append(LIBS = MainLibraryLinks);
sourceFiles = ['main.cpp']
env.Program(target = 'program', source = sourceFiles);

